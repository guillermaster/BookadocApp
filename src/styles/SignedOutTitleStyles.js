import { StyleSheet } from 'react-native';

export let SignedOutTitleStyles = StyleSheet.create({
  title: {
    marginTop: 0,
    fontSize: 42,
    textAlign: 'center',
    fontWeight: '500',
    color: '#fff'
  },
  subTitle: {
    fontSize: 36,
    textAlign: 'center',
    fontWeight: '100',
    marginBottom: 20,
    color: '#fff'
  }
});